# 3D全方位フライトバトル 基本デモ 要件定義書

- 作成日: 2026-07-03（1.0）/ 最終更新: 2026-08-12（5.3）
- 対象フォルダ: `/workspace/3D-world-shooting`
- 版: 5.3

## 変更履歴 5.3（強化アイテム＋当てやすさ・難易度調整・実装済み）

検証サブエージェント（Opus・読み取り専用）で「計測 → 改善提案 → 実行 → 再計測」を3周実施。
最終判定: SHIP（出荷可）。ブロッカーなし。

### 1. 武器強化アイテム（`src/game/systems/items.js` 新規）

`3D_shooting/arwing_react/src/game/systems/items.js` の4種をそのままの効果で移植。
元実装はレール型で「奥から流れてくる」形だったため、全方位向けに
「その場に浮かんで回り、一定時間で消える」形へ作り替えている。

| 種類 | 見た目 | 効果 |
|---|---|---|
| laser | 緑の「L」スプライト | レーザー Lv1→3（Lv2以降は二連射・命中半径に加算） |
| bomb | 赤の「B」スプライト | ボム +1 |
| silver | 水色のリング | シールド +30 回復 |
| gold | 金のリング | 2個で最大シールド ×1.5（上限200） |

- 出現: 敵撃破時に `itemDropChance` 0.38 で落下 ＋ 進行方向の先へ `itemAmbientInterval` 7秒ごとに補充（最大3個）
- 取得: `itemPickupRadius` 26 ＋ `itemMagnetRange` 70 からの吸い寄せ（`itemMagnetPull` 1.2）
- 対戦中はアイテムなし（各自の画面にしか出ず有利不利が生まれるため `G.match.active` で抑止）
- HUDは `LASER LV{n}`（Lv3は青）／ボム数／金リング所持数を表示。シールドバーは `shield/shieldMax` の割合で描画

### 2. 当てやすさ（「レーザーが当たらない」への対応）

原因の切り分けで、判定の広さではなく**弾速不足による偏差（リード）要求**が主因と特定。

- `laserSpeed` 120 → **190**（必要なリード量 34 → 21）
- `enemyHitRadius1/2` 6.0/6.5 → **13.0/13.5**、レーザーLvで最大 +3.0
- 実測: 素直な操作での撃墜数 **1.6 → 25.3 機/分**、命中率 0.9% → 11.3%

### 3. 難易度（「敵の弾が多くてすぐ終わる」への対応）

- `enemyFireCdMin/Max` 0.9/2.0 → **1.8/3.6**、`damageEnemyShot` 7 → **5**
- `enemyShotsMax` **8**（同時に飛ぶ敵弾の上限。弾幕化の防止）
- `playerHitGrace` **0.6秒**（被弾直後の小さな無敵。連続被弾での即死防止）
- 逆に簡単になりすぎた（撃破速度が湧き速度を追い越し、同時出現数が平均2.0機まで低下）ため
  `enemySpawnMin/Max` 2.2/4.2 → **1.1/2.2**。同時出現数 平均6.4〜7.8機に回復
- 生存時間: 2.1分 → 5.2〜8.8分（自動操縦の計測値。実際の人の操作はこれより短い）

### 4. 見やすさ

- レーダーをスマホで **120px → 68px**（面積で約1/3）＋不透明度0.78。PC(170px)は変更なし。3段階のブレークポイント
- レーダーに**強化アイテムを菱形で表示**（敵の丸と形で区別。敵より手前に描画）、画面外は**方向矢印**（最大2本）
- アイテムのスプライトを加算合成 → 通常合成＋濃い縁取りに（明るい空での白飛び対策）
- 銀リングを白 → **水色**（大きさではなく色相の問題。明るい空に溶けていた）
- 敵機の `emissiveMap` を外して自己発光を有効化（宇宙で黒いシルエットになる問題。設定だけでは効かなかった）

### 5. 計測サマリ（3ループ）

| 指標 | ループ1 | ループ3 |
|---|---|---|
| 撃墜数/分（素直な操作・草原） | 1.6 | 25.3 |
| 命中率（素直な操作） | 0.9% | 11.3% |
| アイテム取得率 | 0%（30個中0個） | 36%（80個中29個） |
| Lv3到達までの時間 | 到達せず | 27秒 |
| 同時出現の敵数（平均） | — | 6.4〜7.8機 |
| 描画コール（草原） | 33.1 | 34.7〜37.8（+6.5%） |
| 実行時エラー | 0 | 0 |

## 変更履歴 5.2（アセット圧縮＋バトルの手応え・実装済み）

検証サブエージェント（Opus・読み取り専用）で計測 → 改善 → 再計測を実施。

**軽量化（ゴール2）— 転送量を大幅削減**
1. **全GLBを meshopt圧縮＋テクスチャWebP化**（`@gltf-transform/cli` を devDependency に追加。
   `npx gltf-transform optimize <in> <out> --compress meshopt --texture-compress webp --texture-size 2048 --simplify false`）。
   `public/models` **19MB → 2.2MB**（mountains 3.88MB→413KB / tree1 3.13MB→525KB / tree2 2.57MB→424KB /
   Satellite2 3.39MB→249KB / SPACE2_hdri 1.44MB→61KB / arwing 773KB→72KB）。
   画質はPSNR 31.8〜38.6dBで劣化ほぼ無し（検証で実測）
   - **注意**: 復号が必要。drei の `useGLTF` は自動対応だが、生の `GLTFLoader` を使う `enemies.js` には
     `loader.setMeshoptDecoder(MeshoptDecoder)` が必須
2. **草原の地面テクスチャをWebP化**（1024px・品質82）: 1.9MB → 0.58MB。`environment.jsx` の参照も `.webp` へ
3. **mountains.glb を metal-rough 形式へ変換**（`gltf-transform metalrough`）。
   古い `KHR_materials_pbrSpecularGlossiness` は three r160 が非対応で色テクスチャ463KBが捨てられ警告も出ていた。
   変換により**本来の岩肌の色**が出るようになり、暗くする補正 `mountainTint` は 0.62→1.0 に戻した
4. **タイトル画面ではどちらのステージも読み込まない**（`Environment3D` が `screen==='start'` の間 null）。
   出撃したステージだけを `loaded` ref で保持
5. 未使用の `rock.glb`(1.98MB) と `HDRI_SPACE.glb`(656KB) を `asset_original/` へ退避（public から除外）
6. 結果: **タイトル 4.04MB → 0.14MB / 草原 13.76MB → 3.43MB / 宇宙 10.77MB → 0.76MB（-93%）**、
   `dist` 全体 **24MB → 5.1MB**。圧縮前の原本は全て `asset_original/` に保管

**バトルの手応え（ゴール1）**
7. **敵が撃ち返してくる**（`enemies.js` の `G.enemyShots`）。**偏差撃ち**（弾が届く頃の自機位置を狙う）＋
   着弾点で一定のブレ（`enemyShotMiss` 3.2）。射程190・機首条件cos0.65・間隔0.9〜2.0秒・弾速90・1発7ダメージ。
   命中判定は線分。弾は加算合成の光の弾（半径0.22・長さ5）
8. **被弾方向インジケータ**（`store.damage` = 自機基準の角度、`TargetMarkers.jsx` が赤い弧で表示）
9. **敵AIを「逃げる」→「絡む」に変更**: enemy2は遠いとプレイヤーへ向き直り高度も合わせ、至近では曲がらず抜ける。
   enemy1も `enemyEngageRange`(320) より遠いと緩く向き直る
10. **敵機を大きく**（スケール 2.6/3.0 → 4.2/4.6）
11. 逆光の山が真っ黒に潰れるため `hemisphereLight`(0.55) を追加
12. 結果（キーボードのみ・ゲーム内120秒）: **撃墜 4機 → 21機**、被弾 3分で1発 → **約5.7秒に1発**、
    敵が画面内にいる割合 52.5% → **92.4%**、敵の見かけ幅 11px → **27.6px**、
    回避しないと約90秒で墜落（回避すれば十分持つ）＝理不尽ではない手応えに

**残課題**: `sky.hdr`(1.19MB・草原の最大単品)の削減、敵に役割（編隊/僚機/ボス）を与える、
プレイヤー側の強化要素、バレルロールで弾を弾く、BGM。総合評価は原作を10として**6.5**。

## 変更履歴 5.1（品質改善ループ・スマホ快適化＋SF64的な狙いやすさ・実装済み）

検証サブエージェント（Opus）で計測 → 改善 → 再計測を3周実施。基準は ①スターフォックス64バトルモード級の
快適さ・見やすさ ②スマホWebブラウザでの軽さ。

**見やすさ**
1. **露出調整**: `toneMappingExposure` を草原0.78 / 宇宙1.0（`config.exposureGrass/Space`、SpaceSkyが出入りで切替）。
   空の純白画素 82.2% → **0%**
2. **照準レチクルに縁取り**: 濃色(0x001206)を 1.05倍と0.95倍の2枚重ね、本体は白寄りの緑(0xc8ffd6)。
   明るい背景でのコントラスト 1.19:1 → **7.7〜10.4:1**
3. **雪山を暗く**: マテリアル色を `config.mountainTint` 0.62倍。照準・敵機が白に埋もれるのを解消
4. **上部HUDを1本の横バーに統合**（`Hud.jsx`）＋箱の背景を濃く。390px幅での重なり **121px → 0**
5. **自分の爆風にカメラが入ると薄くなる**（`effects.js` `blastCameraFade`）。ボムに突っ込んでも視界が塞がらない

**狙いやすさ（SF64的な要素）**
6. **ロックオン枠**: 機首前方コーン内の最寄りの敵/対戦相手にオレンジ枠（`targeting.js`、`G.lockTarget`）。
   表示率 **86.6%**
7. **画面外の相手を指す矢印**（`ui/TargetMarkers.jsx`、`store.markers`）。画面端に配置し、L/Rボタン・下部ボタン群・
   レーダーを避ける専用インセット（上56px / 左右44px / 下210px）＋同一点への潰れ防止
8. **命中マーク**（`store.hitMark`）＋撃破音。当てた手応えを可視化
9. **レーザー命中判定を線分判定に**（`effects.segmentPointDistance` / `laserDistanceTo`）。
   低fpsで1フレーム6単位以上進み点判定ですり抜けていた不具合を解消
10. **敵の出現を近く・正面寄りに**（`enemySpawnDist` 380→300、`enemySpawnSpread` 2.4→1.5rad）
   → キーボード操作のみ120秒で **撃墜0機 → 4機**（ゲーム内時間あたりでは約1.5倍のペース）

**スマホ対応・軽量化**
11. **タイトル画面では木と山（9.58MB）を読み込まない**（`environment.jsx` の `GrassStage heavy` ＋独立Suspense）。
    出撃時に読む。宇宙ステージだけ遊ぶ人は一度もダウンロードしない → 宇宙の総アセット **20.34MB → 10.77MB（-47%）**
12. **タッチUI**: 全体50%透過をやめ個別に75%（押下で100%）、`env(safe-area-inset-*)` 対応、
    仮想スティックの反応領域からレーダー矩形を除外、レーダーは幅480px未満で170→120px
13. **操作ガイドをタッチ用文言に分岐**し、位置を左上（PAUSE下）へ・表示10秒→6秒。ボタン/照準/自機との重なり0
14. 描画負荷は据え置き（草原 drawCalls 26–28 / 三角形 63k、宇宙 15–19 / 9.4k–12.9k）、メモリリーク無し

**残課題（未実施）**: モデル/テクスチャ圧縮（Draco・meshopt・KTX2）で草原13.8MB→5MB前後が見込めるが
パッケージ追加が必要なため保留。未使用ファイル `public/models/rock.glb`(1.98MB)、
`public/models/space/HDRI_SPACE.glb`(656KB) も削除候補（未削除）。

## 変更履歴 5.0（第5フェーズ・URL共有マルチプレイ対戦・実装済み）

1. **通信方式**: `trystero` v0.25（WebRTCメッシュ・サーバー不要）。待ち合わせは公開Nostrリレー経由、
   ルームコード4文字（読み間違いにくい31文字から生成）を `?room=XXXX` としてURL共有 → 開いた人が自動で同じ部屋へ。
   静的ファイルのみで完結するため将来 GitHub Pages にそのまま公開可能
   - v0.25 は旧版とAPIが異なる: `makeAction` は `{send, onMessage}` を返す／`onPeerJoin` は代入式／
     個別送信は `send(data, {target: peerId})`
2. **通信コア（`src/net/net.js` 新規）**: 6メッセージ（meta=名前+部屋主 / state=位置向き / fire / hit / down / match）。
   名簿は `NET.players`（Map）、変更は `onRosterChange` リスナー集合で通知。
   進行役（ホスト）= 部屋を作った人（meta.creator）。不在時はpeerId昇順の先頭が自動繰り上げ
3. **自機状態の送信**: GameTick が毎秒12回、位置(小数1桁)+クォータニオン(3桁)+boost+visible を送信
4. **他機表示（`RemotePlayers.jsx` 新規）**: arwing.glbクローンに機体色（青/赤/緑/黄 = peerId昇順）＋
   頭上名前ラベル（drei Html）。位置lerp/向きslerp補間、80超のワープは即時スナップ、6秒無通信で非表示。
   実体は `NET.players[].mesh` に登録（判定・レーダーが参照）。モデル組み立ては Ship.jsx の
   `buildArwingModel(gltfScene, tint)` を共用化
5. **対戦判定（`systems/pvp.js` 新規）**: 命中判定は**撃った側**（距離 < pvpHitRadius 5.0、広め=快適重視）
   → hit通知(dmg25、ボム爆風60・1爆風1人1回) → 受けた側が hitShield。無敵中・墜落中は無効。
   撃墜されたら down通知（by=最後に撃った人）→ 撃った側 kills+1・スコア+500。
   相手の弾・爆風は remote 印付きで見た目のみ（NPC・対人判定から除外）
6. **ルール2種**（ロビーでホストが選択）:
   - 時間マッチ（180秒・壁時計基準）: 撃墜されても3秒で復活（ランダム位置・2.5秒無敵で機体点滅）。
     タイムアップで撃墜数ランキング
   - サバイバル: 復活なし。脱落者は観戦カメラ（生存者を後方から追う）。最後の1人で決着。
     終了判定は「最大生存数2以上→生存1以下」（各自がローカル判定）
7. **画面**: タイトルに「ひとりで遊ぶ / みんなで対戦」。ロビー（名前入力(localStorage記憶)・
   部屋を作る/URL参加・共有URLコピー・参加者一覧(色+★=進行役)・ホストのみルール/ステージ選択とSTART）、
   リザルト（勝者・ランキング・もう一回(ホスト)/ロビーへ/退室）。
   HUD: 残り時間または生存人数（中上）・順位表（右上）・復活カウントダウン・観戦中表示・入退室のお知らせ。
   レーダーに他プレイヤーを機体色の点（8px・白縁）で表示
8. **対戦中はNPC敵機なし**（startGame({npc:false})＋updateEnemiesガード）。ソロは従来通り。
   ESCで退室（部屋からも抜ける）。startMatch時は参加者を半径350の円周上に中心向きで配置
9. 検証: Playwright 2〜3ページ同時で ロビー→開戦→命中→撃墜→復活→TIME UP→リザルト→再戦→退室通知、
   サバイバル3人（脱落→観戦→決着）、ソロ回帰、コンソールエラー0件

## 変更履歴 4.0（第4フェーズ・バトル要素・実装済み）

1. **敵機2種**（`systems/enemies.js` 新規、モデルは 3D_shooting の enemy1/enemy2_spaceship.glb をコピー）
   - 出現: プレイヤーの視界の奥（380先）から自然に湧く。間隔2.2〜4.2秒・同時最大8機・開始時に前方3機
   - enemy1（60%）: まっすぐ直進、境界で内側へ向き直す（速度22）
   - enemy2（40%）: 蛇行（方位・高度がsin波で揺れる）＋プレイヤー接近時は逃げる（速度27）
   - モデルはバウンディング球半径1に正規化→スケール2.6/3.0、rotation.y=π補正（arwing方式）。ロード失敗時は多面体
   - **当たり判定は広め（快適重視）**: レーザー命中半径 6.0/6.5・即撃破。スコア 100/200、撃破音 enemy.mp3
   - ボム爆風（blast.userData.radius）に巻き込まれても撃破。体当たりは敵爆発＋シールド-20
   - 900以上離れた敵は静かに消して近くに湧き直す。レーダーに赤点表示（最大10）
2. **シールド実値化とダメージ**: `hitShield(amount)` 一元管理（flight.js）。障害物バウンド-22、敵体当たり-20。
   HUDバーは実値連動・30以下で赤点滅
3. **墜落シーン（MODE.CRASH）**: シールド0で操作不能→機首下げ・きりもみ回転・重力降下、0.12秒毎に炎パフ。
   地面到達（草原）/2.6秒（宇宙）/障害物直撃で大爆発（maxScale22）→機体非表示→1.4秒後にGAME OVER。
   カメラは定点lookAtで見送り（宙返りと同じ酔わない演出）
4. **GAME OVER画面**: screen:'over'。MISSION FAILED＋SCORE＋撃墜数＋RETRY（同ステージ即再開）/TITLE
5. **カメラ最接近**: camBack 7.2→**3.1** / camUp 2.5→**1.05** / camBackBoost→4.2 / lookAhead→8（原作写真準拠）
6. **U-TURNボタンを右下へ**: 非タッチ=right18/bottom24、タッチ=LOOP/BOMB/LASERクラスタの真上（bottom240）
7. effects.spawnBlast をオプション対応（maxScale/duration/coreColor）、小型爆発 explodeSmall を追加

## 変更履歴 3.0（第3フェーズ・宇宙ステージ＋UI改善＋快適化・実装済み）

1. **宇宙ステージ＋ステージセレクト**: スタート画面に「STAGE SELECT」（草原/宇宙の2枚カード、
   CSSグラデーション見本）。`gameStore.stage` → `control.startGame` が `G.stageCfg`（STAGES）を反映。
   環境は `environment.jsx`（草原=GrassStage）と `environmentSpace.jsx`（宇宙）で出し分け（未選択はアンマウント）
   - 背景: `asset/Spaceset/HDRI_SPACE.glb`（宇宙の絵の球体モデル、絵は **emissiveMap** に入っている）を
     半径3000へ拡大し unlit・両面で内側表示。同テクスチャを equirect として scene.environment（環境光）にも使用
   - 隕石: `asteroid.glb`（=3D_shooting の asteroide_low_poly）を InstancedMesh 50個、
     半径120〜760・高度±220にランダム配置（開始地点120は空ける）
   - 衛星: ISS（Satellite2、大きさ60）とハッブル（Satellite1、28）を各1基固定配置・ゆっくり自転（少なめ指定）
   - 隕石・衛星は山と同じ **G.colliders**（旧G.mountains を一般化）に登録され、はじかれ判定が効く
     （InstancedMesh でも three-mesh-bvh のレイキャストが有効なことを検証済み）
   - 高度範囲はステージ別: 草原 5〜150 / 宇宙 **-250〜250**（下にも飛べる）。フォグは宇宙では無し
2. **U-TURNボタン/Uキー**: どこでもインメルマン帰還（`flight.startUturn`、NORMAL/ROLLING時のみ）。
   ボタンは右端・SHIELD下
3. **レーダーを左下へ移動**（タッチLボタンとの重なり解消）＋自機三角を拡大（8/16px）。
   操作ガイド（開始10秒）は中央下へ移動
4. **高度メーター**: レーダー右脇の縦バー。ステージの高度範囲で正規化し金色マーカー表示（radar.alt）
5. **カメラ近接化（臨場感）**: camBack 9.5→7.2 / camBackBoost 11→8.8 / camUp 3.2→2.5 / lookAhead 12→10
6. **ブースト時の視野拡大**: fov 70⇔76 をlerp（スピード感）
7. **衝突フラッシュ**: はじかれた直後0.45s、画面の縁が赤く光る（store.flash）
8. ESCキーをラッチ方式に（低fps環境で1フレーム内の押下を取りこぼしていたのを修正）

## 変更履歴 2.0（第2フェーズ・実装済み）

1. **武器・アクションを arwing_react に完全準拠**: レーザー（Space/J/クリック、CD 0.30s・速度120）、
   ボム（B/K、3発・1.0sで自動爆発・再押しで手動起爆・多層ブラスト）、バレルロール（Q/E・←→2度押しで720°/0.5s）、
   宙返り（I、2.8s・半径9）、緑二重照準レチクル（38/50）、タッチUI（仮想スティック+L/R/LOOP/BOMB/LASER）、
   効果音（laser/bomb/roll.mp3）、HUD（SCORE/PAUSE/WAVE・LASER・BOMB/SHIELD）
2. **キー変更**: Space=レーザー（旧ブレーキ）、ブレーキは Ctrl へ
3. **カメラ水平維持（酔い対策）**: up恒久ワールド上・後方位置はヨーのみ追従。左右移動・ロールで地平線が傾かない
4. **高度上限**: maxAltitude=300 のソフト天井（下限5は継続）
5. **木の直立補正**: tree1/2.glbのルートに焼き込まれたX-90°回転をラッパーGroupのX+90°で相殺
6. **レーダー追加**: 画面左端の正方形（枠=フィールド境界±1000）。黄色三角=自機（機首方位に回転）、境界外で赤色化
7. **WARNING表示を完全削除**（store/HUD/flightから除去）
8. **境界帰還をインメルマンターン化**: 半ループ(1.6s・半径16)→ロールアウト(0.7s)→中心方位へ収束(≦1.5s)。
   演出中カメラは定点+lookAtのみ（回転運動しない）→ロールアウト以降に新方位後方へ回り込み（camBack 13）
9. HUDのSPEED表示・AREAバーは削除（添付1準拠レイアウトへ、位置把握はレーダーが担当）

## 変更履歴 2.1（微修正・実装済み）

1. ピッチ操作を反転（invertPitch=true）: ↑/W=上昇、↓/S=下降
2. 木の直立補正の回転方向を修正（+90°→**-90°**。+90°では根本が上向きに反転していた）
3. 地面の縁が見えないよう調整: maxAltitude 300→**150**、fieldRadius 1000→**800**、
   groundRadius 1200→**1600**、fog 500/2800→**350/2200**
4. **山との衝突（はじかれ判定）を追加**: three-mesh-bvh（dreiの同梱依存）でBVH構築し、
   毎フレーム移動線分をレイキャスト→面法線で進行方向を反射・速度45%残し・衝突音。
   演出（宙返り/帰還）中の衝突は演出を中断して通常操作へ（flight.js resolveMountainCollision）

※以降の章は1.0の仕様。上記変更が優先される。
- 本書の位置づけ: **このまま開発者（またはAI）に渡せば実装を開始できる実装指示書**

---

## 1. 概要

### 1.1 目的

『スターフォックス64』のバトルモードのような、**3D空間を全方向に自由に飛び回れるフライトバトルの基本デモ画面**を構築する。
本フェーズは「気持ちよく飛べる土台」の完成が目的であり、**ボス戦は含まない**。敵AI・武器などの戦闘要素は次フェーズとする。

### 1.2 成果物

- ブラウザで動作する3Dフライトデモ（`npm run dev` で起動、単一ページ）
- プレイヤーは草原・木・山・空（HDRI）のある3D世界を、機体（アーウィン風戦闘機）で自由に飛行できる
- ステージ境界に達すると自動でUターンし、警告表示が出る

### 1.3 対象環境

- PCブラウザ（Chrome / Edge 最新版、WebGL2必須）
- 操作: キーボード（タッチ操作は将来対応枠として設計だけ考慮しておく）
- 目標フレームレート: 60fps

### 1.4 スコープ外（次フェーズ）

- ボス戦、敵機AI、武器（レーザー/ボム）、ダメージ・スコア計算
- マルチプレイ、ステージ選択、セーブ機能

---

## 2. 技術スタック

既存プロジェクト `/workspace/3D_shooting/arwing_react` と**同一構成**とする（コード・UIの流用性を最優先）。
言語は JavaScript（JSX）。TypeScriptは採用しない。

| 種別 | パッケージ | バージョン（arwing_react準拠） |
|---|---|---|
| ビルドツール | vite | 5.4.11 |
| UI | react / react-dom | 18.3.1 |
| 3Dエンジン | three | 0.160.1 |
| Reactブリッジ | @react-three/fiber | 8.17.10 |
| 3Dユーティリティ | @react-three/drei | 9.114.0 |
| 状態管理 | zustand | 4.5.5 |
| スタイル | tailwindcss | 3.4.17 |
| Reactプラグイン | @vitejs/plugin-react | 4.3.4 |

> セットアップ手順: arwing_react の `package.json` / `vite.config.js` / `tailwind.config.js` / `postcss.config.js` /
> `index.html` を雛形としてコピーし、プロジェクト名だけ変更するのが最短。

---

## 3. 再利用アセット一覧

### 3.1 コード・モデル（流用元: `/workspace/3D_shooting/arwing_react`）

| 流用元 | 新配置先 | 用途・備考 |
|---|---|---|
| `public/models/arwing.glb` (772KB) | `public/models/arwing.glb` | プレイヤー機体 |
| `src/game/Ship.jsx` | `src/game/Ship.jsx` | 機体読込・フィット処理（rotationY=π、Yオフセット-0.3、スケール1.15、最長辺3.5正規化）・エンジングロー（シアン加算合成球×2）をそのまま流用 |
| `src/game/systems/freeflight.js` | `src/game/systems/flight.js` | **本デモの核**。既に3軸制御＋球状境界＋slerp自動Uターンの実験実装がある。これを昇格・拡張する（§4.1） |
| `src/game/systems/ship.js` の `updateCamera` | `src/game/systems/camera.js` | チェイスカメラ（§4.4） |
| `src/input/input.js` | `src/input/input.js` | キーボード入力管理 |
| `src/ui/Hud.jsx` | `src/ui/Hud.jsx` | WARNING演出・ネオン風CRTスタイルを流用（不要要素は削除、§4.5） |
| `src/ui/Overlays.jsx` | `src/ui/Overlays.jsx` | タイトル画面（"MISSION START"ボタン）のみ流用 |
| `src/audio/audio.js` + `public/audio/bgm_*.mp3, roll.mp3` | 同構成 | BGMループ・操作音。**任意（後回し可）** |
| `src/game/state.js` / `src/store/gameStore.js` | 同構成 | フレーム用ミュータブル状態とUI用Zustandの分離パターンを踏襲 |

### 3.2 環境素材（流用元: `/workspace/3D-world-game/assets`）

> 流用元は Godot（別ゲームエンジン）のプロジェクトだが、**素材ファイル自体はThree.jsでそのまま読める**。
> `*.glb:Zone.Identifier` という付随ファイルはWindowsのダウンロード印なのでコピーしない。

| 流用元 | 新配置先 | 用途 |
|---|---|---|
| `sky.hdr` (1.2MB) | `public/env/sky.hdr` | HDRI環境マップ（空・環境光） |
| `background_mountains.glb` (3.7MB) | `public/models/mountains.glb` | 遠景の山並み |
| `props/tree1.glb` (3.0MB) / `props/tree2.glb` (2.5MB) | `public/models/tree1.glb` 等 | 地上の木 |
| `props/rock.glb` (1.9MB) | `public/models/rock.glb` | 岩（任意） |
| `grass_diff.jpg` / `grass_nor.jpg` / `grass_rough.jpg` | `public/textures/` | 地面のPBRテクスチャ（色/凹凸/粗さ） |

コピーコマンド例（実装開始時に実行）:

```bash
mkdir -p public/models public/env public/textures
cp /workspace/3D_shooting/arwing_react/public/models/arwing.glb public/models/
cp /workspace/3D-world-game/assets/sky.hdr public/env/
cp /workspace/3D-world-game/assets/background_mountains.glb public/models/mountains.glb
cp /workspace/3D-world-game/assets/props/tree1.glb /workspace/3D-world-game/assets/props/tree2.glb public/models/
cp /workspace/3D-world-game/assets/grass_*.jpg public/textures/
```

モデル合計 約12MB。初回ロード目安5秒以内（ローカル）。超える場合はDraco圧縮を検討（次フェーズ）。

---

## 4. 機能要件

### F-1 プレイヤー機体の飛行物理と角度制限

**入力**

| キー | 動作 |
|---|---|
| W / ↑ | ピッチダウン（機首下げ）※設定で反転可 |
| S / ↓ | ピッチアップ（機首上げ） |
| A / ← | 左ロール → 左旋回 |
| D / → | 右ロール → 右旋回 |
| Shift | ブースト（加速） |
| Space | ブレーキ（減速） |

**仕様**

1. 機体は**常に前進**する（速度0にはならない）。
2. **ピッチ角制限**: 機体の上下傾きを **±60°（±π/3 rad）にクランプ**する。クォータニオンから毎フレームピッチ角を取り出し、超過分を打ち消す（またはオイラー角管理でクランプ後に再構成する）。
3. **ロール→ヨー連動**（本デモの飛行感の要）:
   - 左右キー入力で、まず機体がロール（傾き）する。目標ロール角へ `lerp` で滑らかに到達。
   - そのロール角に**比例して**ヨー（Y軸旋回）が発生する: `yawRate = -sin(rollAngle) * yawFactor`
   - キーを離すとロールが水平へ自動復帰し、旋回も止まる。
4. 実装は `freeflight.js` の3軸制御（pitch 1.7 / yaw 1.4 / roll 2.6 rad/s）を出発点とし、上記の連動式に改修する。

**数値パラメータ初期値**（§7の一覧にも掲載。実装後にプレイして調整すること）

| 項目 | 初期値 | 出典 |
|---|---|---|
| 通常速度 | 30 単位/秒 | 3D-world-game `arwing.gd` |
| ブースト速度 | 56 単位/秒 | 同上 |
| ブレーキ速度 | 13 単位/秒 | 同上 |
| 加減速の補間 | `speed = lerp(speed, target, 3.0*dt)` | 新規 |
| ピッチ回転速度 | 1.5 rad/s | arwing.gd（freeflight.jsは1.7） |
| 最大ロール角 | 65°（±） | 新規 |
| ロール到達補間 | lerp係数 0.15/フレーム相当 | arwing_react `ship.js` のバンク処理 |
| ヨー係数 yawFactor | 1.3 rad/s（フルロール時） | arwing.gd YAW_RATE |
| ピッチクランプ | ±60° | 本仕様の必須要件 |

### F-2 環境設定（HDRIとライティング）

1. **HDRI読込**: `public/env/sky.hdr` を読み込み、背景（`scene.background`）と環境光（`scene.environment`）に設定する。
   - R3Fでは drei の `<Environment files="/env/sky.hdr" background />` を第一候補とする（内部でRGBELoaderを使用）。
   - 素のThree.jsで書く場合の等価コード（参考・フォールバック判定にも使う）:
     ```js
     import { RGBELoader } from 'three/examples/jsm/loaders/RGBELoader.js';
     new RGBELoader().load('/env/sky.hdr', (tex) => {
       tex.mapping = THREE.EquirectangularReflectionMapping;
       scene.background = tex;
       scene.environment = tex;
     }, undefined, () => applyFallbackSky(scene)); // 読込失敗時
     ```
2. **フォールバック**（HDRIが無い/読めない場合に自動で切替）:
   ```js
   function applyFallbackSky(scene) {
     scene.background = new THREE.Color(0x0a0e2a);          // 深い宇宙紺
     scene.fog = new THREE.Fog(0x0a0e2a, 300, 1400);        // 奥行きのかすみ
     // 星: 2000点のPointsを半径1500の球面にランダム配置
   }
   ```
3. **レンダラー設定**（必須）:
   - `renderer.outputColorSpace = THREE.SRGBColorSpace`
   - `renderer.toneMapping = THREE.ACESFilmicToneMapping`
   - R3Fでは `<Canvas gl={{ ... }} onCreated={({gl}) => {...}}>` で設定（R3F v8はデフォルトでほぼこの設定だが、明示すること）。
4. **光源**: HDRIの環境光に加え、`DirectionalLight` を1灯配置。
   - 色 `#fff5e0`（暖色）、強度 1.0、方向はHDRIの太陽位置に合わせる（初期値: 上空斜め45°）
   - `castShadow = true`、shadowMap有効（`renderer.shadowMap.enabled = true`、PCFSoftShadowMap、マップサイズ2048）
   - 影を受けるのは地面のみ（負荷対策。木・山は受けない）
5. **地形・配置物**:
   - 地面: 半径1200の円形（または2400×2400平面）。grass PBRテクスチャ3枚（diff/nor/rough、リピート数48程度）を適用
   - 山: `mountains.glb` を遠景に1周分配置（スケールと沈み込みは見た目で調整。Godot版では0.08倍・基部沈下で稜線だけ見せていた）
   - 木: tree1/tree2 を地面にランダム配置 計60〜100本。**`InstancedMesh`（またはdrei `<Instances>`）で描画**し、draw callを抑える
   - Fog: 大気感のため `THREE.Fog(空に馴染む色, 400, 1600)` を薄くかける

### F-3 フィールド境界と自動Uターンシステム

1. ステージは**原点(0,0,0)中心・半径1000の球体エリア**。
2. さらに高度制限: 地面が存在するため **最低高度 5**（地面高+5、本デモは平坦地形なので y≧5 で可）を設ける。地面接近時は機首を自動的にやや上向きへ補正する。
3. **Uターン発動**: 機体の `position.length() > 1000` になったら Uターン状態へ移行。
   - プレイヤーの回転入力を無効化（またはブレンド率を0.2まで低下）
   - 目標姿勢 = 「機首が原点(0,0,0)を向く」クォータニオンを算出し、
     `ship.quaternion.slerp(targetQuaternion, turnRate * dt)` （turnRate初期値 1.2）で滑らかに反転
4. **解除条件**: 機首と「中心方向ベクトル」のなす角が 20°未満 かつ `position.length() < 950` で通常操作へ復帰（ヒステリシスを設けて境界でのバタつきを防ぐ）。
5. **警告表示**: Uターン中は画面中央に **`WARNING: RETURNING TO MISSION AREA`** をHTML/CSSで表示。
   - arwing_react `Hud.jsx` のWARNING演出（赤い大文字＋画面全体の赤い脈動）をそのまま流用
   - Uターン解除で即消灯
6. 参考実装: `freeflight.js` に「球状境界（半径150）＋slerpによる自動Uターン」の同型ロジックが既にある。半径・閾値を本仕様値に差し替えて流用する。

### F-4 カメラ挙動（チェイスカメラ）

1. カメラは**機体の子要素にしない**。シーン直下の独立オブジェクトとする。
2. 毎フレーム、次の手順で遅延追従させる（arwing_react `ship.js` の `updateCamera` 方式を踏襲）:
   ```js
   const back = new THREE.Vector3(0, 0, camBack).applyQuaternion(ship.quaternion);
   const up   = new THREE.Vector3(0, camUp, 0).applyQuaternion(ship.quaternion);
   camera.position.lerp(ship.position.clone().add(back), camLerp);
   camera.up.copy(up);                       // ロール時に地平線が傾く＝飛行感
   camera.lookAt(ship.position.clone().add(forward.multiplyScalar(lookAhead)));
   ```
3. パラメータ初期値: `camBack = 9.5`、`camUp = 3.2`、`camLerp = 0.10〜0.15`、`lookAhead = 12`（機体の少し前方を見る）。
4. ブースト中は `camBack` を 11 まで `lerp` で伸ばし、加速感を出す（任意・推奨）。

### F-5 HUD / UI

| 要素 | 表示 | 流用元 |
|---|---|---|
| タイトル画面 | タイトル文字＋操作説明＋「MISSION START」ボタン | `Overlays.jsx` |
| 速度表示 | 左下に `SPEED [数値]`（ブースト中は色変化） | `Hud.jsx` のスタイル |
| WARNING | 中央・赤・脈動（F-3連動） | `Hud.jsx` の announce/warning |
| 境界距離バー | 右上に「中心からの距離/1000」の横バー（90%超で赤） | `Hud.jsx` のシールドバーを転用 |
| 操作ガイド | 起動後10秒だけ左上に薄く表示 | 新規（数行） |

**削除する要素**（arwing_reactから持ち込まない）: スコア、ウェーブ、レーザーLV、ボム、ボスHP、ステージ進捗バー、照準レチクル。

---

## 5. 非機能要件

| 項目 | 要求 |
|---|---|
| フレームレート | 60fps（ノートPC内蔵GPUで45fps以上） |
| 初回ロード | ローカルで5秒以内（モデル合計約12MB） |
| draw call | 木はInstancedMesh必須。全体で150 call以下目安 |
| 対応環境 | WebGL2必須。非対応時はエラーメッセージを表示して終了 |
| コード品質 | 毎フレームの `new Vector3()` 生成を避け、モジュールスコープの一時ベクトルを再利用（arwing_reactの流儀に合わせる） |

---

## 6. ディレクトリ構成

```
3D-world-shooting/
├── 要件定義書.md            # 本書
├── index.html
├── package.json / vite.config.js / tailwind.config.js / postcss.config.js
├── public/
│   ├── models/   (arwing.glb, mountains.glb, tree1.glb, tree2.glb, rock.glb)
│   ├── env/      (sky.hdr)
│   ├── textures/ (grass_diff.jpg, grass_nor.jpg, grass_rough.jpg)
│   └── audio/    (任意: bgm_intro.mp3, bgm_loop.mp3, roll.mp3)
└── src/
    ├── main.jsx             # エントリーポイント
    ├── App.jsx              # Canvas + UIレイヤ構成
    ├── index.css
    ├── game/
    │   ├── config.js        # §7の全パラメータを一元管理（マジックナンバー禁止）
    │   ├── state.js         # フレーム用ミュータブル状態 G
    │   ├── GameTick.jsx     # useFrameの唯一の呼び出し元（中央ループ）
    │   ├── Ship.jsx         # 機体モデル読込・エンジングロー
    │   └── systems/
    │       ├── flight.js    # 飛行物理・角度制限・境界Uターン（freeflight.js改修）
    │       ├── camera.js    # チェイスカメラ
    │       └── environment.jsx # HDRI・ライト・地面・木・山・Fog・フォールバック
    ├── input/
    │   └── input.js         # キーボード入力（流用）
    ├── store/
    │   └── gameStore.js     # UI用Zustand（画面遷移・warning・speed表示）
    └── ui/
        ├── Hud.jsx          # 速度・WARNING・距離バー
        └── Overlays.jsx     # タイトル画面
```

> 元仕様文の `main.ts / flightSystem.ts / environment.ts / ui.ts` の4分割は、
> React構成では上記の `flight.js / camera.js / environment.jsx / Hud.jsx` に対応する。

---

## 7. 数値パラメータ一覧（config.js に定義）

```js
export const CONFIG = {
  // 速度
  speedNormal: 30, speedBoost: 56, speedBrake: 13, speedLerp: 3.0,
  // 回転
  pitchRate: 1.5, rollMax: (65 * Math.PI) / 180, rollLerp: 0.15,
  yawFactor: 1.3, pitchClamp: Math.PI / 3, // ±60°
  // 境界
  fieldRadius: 1000, uturnExitRadius: 950, uturnRate: 1.2,
  uturnExitAngle: (20 * Math.PI) / 180, minAltitude: 5,
  // カメラ
  camBack: 9.5, camBackBoost: 11, camUp: 3.2, camLerp: 0.12, lookAhead: 12,
  // 環境
  fogNear: 400, fogFar: 1600, treeCount: 80, shadowMapSize: 2048,
};
```

---

## 8. 開発ステップ（この順で実装し、各ステップで動作報告する）

| # | 内容 | 完了条件 |
|---|---|---|
| 1 | 雛形作成: arwing_reactの設定ファイル流用、`npm install`、アセットコピー（§3.2のコマンド） | `npm run dev` で空のCanvasが表示される |
| 2 | 機体表示＋飛行物理: Ship.jsx流用、flight.js（前進・ピッチ±60°クランプ・ロール→ヨー連動・加減速） | キーボードで全方向に飛べ、真上/真下を向けない（±60°で止まる） |
| 3 | チェイスカメラ: camera.js | 機体後方から遅延追従し、ロール時に画面が傾く |
| 4 | 環境: HDRI（＋フォールバック）、SRGB/ACES設定、DirectionalLight＋影、地面・木・山・Fog | 空がHDRIで映り、地面に機体の影が落ちる。hdrを一時的にリネームしてもフォールバック背景で動く |
| 5 | 境界Uターン＋WARNING表示 | 半径1000を超えると操作が効かなくなり自動反転、WARNING点滅、戻ると復帰 |
| 6 | HUD仕上げ・タイトル画面・（任意）BGM | §4.5の全要素が表示され、60fps近辺で安定 |

各ステップの原則:
- 動くことを確認してから次へ進む（インクリメンタル開発）
- パラメータは必ず `config.js` 経由。ハードコード禁止
- 既存ファイルを流用する際は**コピーして**改変（arwing_react本体は変更しない）

---

## 9. 動作確認方法

### 9.1 手動確認

`npm run dev` → ブラウザで以下を目視確認:

1. ピッチ上限: 上キー押し続け → 機首が60°で止まり宙返りしない
2. 旋回: 左キー押し続け → 傾いてから曲がり始め、離すと水平に戻る
3. Uターン: 直進し続け → WARNING表示とともに自動で中心へ反転、表示が消えて操作復帰
4. カメラ: 急旋回時にカメラが一拍遅れてついてくる（浮遊感）
5. 環境: 空がHDRI、地面に影、遠くが霞む

### 9.2 自動確認（スクリーンショット検証）

この環境には権限なしでブラウザ自動確認ができる共通ツールがある:

```bash
bash ~/.claude/tools/playwright/setup.sh          # コンテナ起動ごとに1回
bash ~/.claude/tools/playwright/with_browser.sh python3 <確認スクリプト.py>
```

- スクリプトは `headless=False` + `executable_path=$PW_CHROME` + `args=$PW_CHROME_ARGS.split()` で起動（WebGL必須のため）
- 確認項目: ①タイトル画面表示 ②START後に機体・地面・空が描画される ③キー送信後に画面が変化する ④コンソールエラーが0件

---

## 10. 用語集（非エンジニア向け）

| 用語 | 意味 |
|---|---|
| ピッチ | 機首の上げ下げ（うなずく方向の傾き） |
| ロール | 左右への傾き（自転車で曲がるときに体を倒すイメージ） |
| ヨー | 左右への向き変え（首を横に振る方向） |
| HDRI | 全方向を写した特別な空の写真。背景と照明を一度に担う |
| PBR | 物理的に正しい質感表現。色・凹凸・ツヤの3枚の画像で本物らしく見せる |
| クォータニオン / slerp | 3Dの向きを表す数学的な仕組みと、その向きを滑らかに変える計算 |
| lerp | 2つの値の間を少しずつ近づける計算（滑らかな追従に使う） |
| Fog（フォグ） | 遠くをかすませる効果。奥行き感が出て、描画の負担も減る |
| InstancedMesh | 同じ形（木など）を大量にまとめて一度に描く高速化の仕組み |
| draw call | 「描いて」という命令の回数。少ないほど軽い |
